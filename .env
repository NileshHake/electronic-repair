PORT=5000

DB_HOST=mysql           # Must match the mysql service name in docker-compose
DB_USER=repair_user
DB_PASSWORD=repair_pass  # ⚠ match this with docker-compose environment
DB_NAME=repaircrm_db     # ⚠ match this with docker-compose environment
DB_PORT=3306             # Internal MySQL port inside Docker

JWT_SECRET=CHANGE_THIS_TO_LONG_RANDOM_STRING
